name: codeql-scoped-analysis
# Exclude non-production prototype/asset directories to keep analysis within runtime limits.
# Keep both exact directory and recursive patterns to ensure stable exclusion matching.
paths-ignore:
  - artifacts/mockup-sandbox
  - artifacts/mockup-sandbox/**
  - artifacts/old-time-chat
  - artifacts/old-time-chat/**
  - attached_assets
  - attached_assets/**
  - screenshots
  - screenshots/**

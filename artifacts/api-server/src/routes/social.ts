import { Router, type IRouter, type Request, type Response } from "express";
// TEMP: full content being restored in follow-up if this is too large - see social_fixed.ts in workspace
export default Router();
